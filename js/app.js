console.log('yo')
// combine objects, arrays, and functions
const curbArray = [
	characters: {
		name1: 'jeff',
		name2: 'larry',
		name3: 'susie',
		name4: 'cheryl',
		name5: 'funkhouser'
	}
]
console.log(curbArray.name5)




let curbObj = {
	funny: true,
	characters: ['larry', 'jeff', 'cheryl', 'susie', 'funkhouser']
};
console.log(curbObj.characters[4])

let curb2 = [
	characters: ['larry', 'jeff', 'cheryl', 'susie', 'funkhouser']
	seasons: ['season 1', 'season 2', 'season 3', 'season 4', 'season 5', 'season 6', 'season 7', 'season 8', 'season 9'  ]

]
console.log(curb2[1][5])

// combine objects, arrays, and functions

// combine objects, arrays, and functions at more than 1 level





// combine objects, arrays, and functions at more than 1 level


// Create callback



// create callback


// indentation
if(true){
  
  const a = 2 + 2;
  
console.log(a);
 }

if(true){
  if(false){
     console.log('hi');
   }            
}
// indentation

// Semantic naming of variables

const evenNums = [2, 4, 6, 8, 10,]

// semantic naming of variables

// Function definition placement
const foo = ()=>{
	console.log('foo here');
}
const bar = ()=>{
    console.log('bar here');
}

bar();
foo();
// function definition placement

// commenting code

const addTwoNums = (firstNum, secondNum)=>{ //function addTwoNums sets parameters for first and second nums
    const finalValue = firstNum + secondNum; //finalValue adds first and second nums
    return finalValue; //returns sum of first and second nums
}

// commenting code

// Error Reading

// it's a syntax error due to calling the function foo before the function has been set

// Error reading













